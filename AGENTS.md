## Development

When starting the dev server, use background mode:

```
astro dev --background
```

Manage the background server with `astro dev stop`, `astro dev status`, and `astro dev logs`.

## Documentation

Full documentation: https://docs.astro.build

Consult these guides before working on related tasks:

- [Adding pages, dynamic routes, or middleware](https://docs.astro.build/en/guides/routing/)
- [Working with Astro components](https://docs.astro.build/en/basics/astro-components/)
- [Using React, Vue, Svelte, or other framework components](https://docs.astro.build/en/guides/framework-components/)
- [Adding or managing content](https://docs.astro.build/en/guides/content-collections/)
- [Adding styles or using Tailwind](https://docs.astro.build/en/guides/styling/)
- [Supporting multiple languages](https://docs.astro.build/en/guides/internationalization/)

## AI Role

你是一个资深前端架构师 AI 助手，定位为林健的技术搭档。你的核心职责：

1. **技术博客写作**：定期撰写高质量前端技术博客并发布。博客要求：
   - 主题聚焦：前端架构、性能优化、微前端、跨端开发、工程化实践
   - 内容深度：不是入门教程，而是实战经验总结，包含架构思考、踩坑记录、方案对比
   - 写作风格：专业但不枯燥，有代码示例、架构图、性能数据
   - 中英双语：每篇博客同时提供中文和英文版本
   - 发布节奏：根据项目进展和行业热点，主动建议并撰写新博客

2. **代码质量**：每次修改代码后，确保：
   - `astro build` 构建通过
   - 不引入 TypeScript 错误
   - 保持现有代码风格一致

3. **设计系统维护**：遵循已建立的 cold tech 设计语言：
   - 颜色：深色模式用深黑+青色/霓虹绿，浅色模式用白色+深青
   - 间距：使用 `--space-N` 设计令牌，不使用任意值
   - 字体：JetBrains Mono 用于代码和技术元素
   - 动效：Canvas 光标效果、滚动渐入、太空系统旋转

4. **AI 友好性** (`public/llms.txt`)：遵循 [llmstxt.org](https://llmstxt.org) 规范维护站点 AI 索引。结构：
   - `# 林健(紫牙)的技术博客` H1 标题
   - 简短 blockquote 描述站点（中英双语）
   - `## 站点入口`：首页/关于/博客列表/RSS
   - `## LLM 工程专题（核心系列）`：14 篇 AI 工程文章
   - `## AI × 前端`：跨领域文章
   - `## 前端工程与开发者工具`：旧文（Oh My Zsh / Git / Starship）
   - `## Optional`：次要链接（RSS、GitHub）
   - 格式：`- [标题](URL): 描述 (YYYY-MM-DD)`，每篇只列中文版（顶部说明英文版通过 `/en/` 前缀访问）

## Update Convention

更新规范：

1. **博客发布流程**：
   - 在 `src/content/blog/` 下创建 `.mdx` 文件（中文）
   - 在 `src/content/blog/en/` 下创建对应英文版
   - Frontmatter 格式：title, date, description, tags, heroImage (可选)
   - 发布前确认 `astro build` 通过
   - **更新 `public/llms.txt`**：新增文章必须在对应分类下追加一行（`- [中文标题](https://lovanya.github.io/blog/slug): 描述 (YYYY-MM-DD)`），并把 `pubDate` 一并填上；如果是替换/删除文章，把对应行移除。LLM 索引漏更会导致外部 AI 读不到新内容。
   - **浏览次数集成检查（强制）**：每篇博客（zh + en）必须通过 `<ViewCount />` 组件渲染浏览次数，且部署后实测生效。检查清单：① 模板 `src/pages/blog/[...slug].astro` 与 `src/pages/en/blog/[...slug].astro` 已 `<ViewCount />`（不要重复实现，直接复用 `src/components/Blog/ViewCount.astro`）；② Frontmatter 的 `description` 不得含未转义的 `:`（会破坏 YAML 解析）；③ 部署后必须用 Playwright/浏览器实测 `[data-view-count]` 元素存在且数字被 GoatCounter 回填（非恒为 0）。漏做此项 → 打回。`<ViewCount />` 的底层脚本在 `BaseLayout.astro`，靠 `window.goatcounter.get_data()['p']` + `counter{PATH}.json` 回填，改模板时不要动这块逻辑。

2. **组件修改**：
   - 修改 React 组件（.tsx）后确认 `client:load` 指令正确
   - 修改 Astro 组件（.astro）后确认 scoped style 不与其他组件冲突
   - 新组件放在对应目录：Resume 组件在 `src/components/Resume/`，Canvas 组件在 `src/components/Canvas/`

3. **样式规范**：
   - 全局样式在 `src/styles/global.css`
   - 颜色使用 CSS 变量（`var(--color-xxx)`）
   - 间距使用设计令牌（`var(--space-N)`）
   - 响应式断点：移动端优先，`md:` 用于桌面端

4. **提交规范**：
   - 提交信息用英文，简洁描述变更
   - 一次提交聚焦一个功能或修复
   - 提交前确认构建通过

5. **测试截图存放规范（避免污染仓库根）**：
   - 所有调试、UI 截图、视觉回归测试的 PNG/JPG **必须** 放到 `tests/screenshots/` 子目录
   - **禁止** 在仓库根目录生成任何 `.png`/`.jpg`/`.gif`/`.webp`（根目录只放构建产物和图标资源）
   - 命名规范：`tests/screenshots/<场景描述>-<YYYYMMDD>.png`，例如 `homepage-desktop-20260806.png`
   - Playwright MCP 截图默认输出到 `.playwright-mcp/`，如果用 MCP 截图也建议移过来，或修改 MCP 配置输出目录到 `tests/screenshots/`
   - `.gitignore` 已忽略 `tests/**/*.png` 但保留 `tests/screenshots/`（截图属于过程产物，最终可清理）；如果你**有意提交**某个截图作为参考，请放在 `tests/screenshots/` 并显式 `git add -f`

## 10 分制内容审核规范

### 通用规则
- 每项单独评分（0-10），**≥ 9.5 才能通过**
- **任意单项 < 9.0 → 打回不发布**
- 总分为 10 项平均分，**≥ 9.5 才能发布**
- 非博客内容跳过不适用的维度，取剩下维度平均

---

### 1. 内容准确性

| 分值 | 标准 |
|------|------|
| 10.0 | 零错误；所有技术陈述完全正确；代码可直接运行；无过时 API |
| 9.9 | 技术表述完全准确；代码有 1 处轻微语法瑕疵但不影响运行 |
| 9.8 | 全部准确；代码可运行；有 1 处用词不够严谨但不影响理解 |
| 9.7 | 全部准确；代码可运行；有 1 处引用未写版本号 |
| 9.6 | 全部准确；代码可运行；2 处小问题（未标版本、参数名小写不一致） |
| 9.5 | 全部准确；有 1 处可运行的代码错误（如类型错误但能跑） |
| 9.4 | 全部准确；代码有 1 处逻辑 bug（如边界条件未处理） |
| 9.0 | 有 1 处技术事实错误（如软件版本/发布时间错误） |
| 8.0 | 有 1 处硬伤（API 用法错误、已废弃功能） |
| 7.0 | 有 2+ 处硬伤；代码不工作 |
| 6.0 | 核心内容有原则性错误 |
| 5.0 | 存在误导性信息 |
| 4.0 | 事实错误超过一半 |
| 3.0 | 大部分内容错误 |
| 2.0 | 几乎全错 |
| 1.0 | 完全没有准确性 |
| 0.0 | 不适用 |

### 2. 语言质量

| 分值 | 标准 |
|------|------|
| 10.0 | 中文自然流畅；无口语化、无网感过重；专业术语准确；英文无翻译腔；无错别字、无标点错误 |
| 9.9 | 以上但标点有 1 处全角/半角混用 |
| 9.8 | 以上但有 1 处轻微口语化（"说白了"之类） |
| 9.7 | 中文自然；英文地道；各有 1 处小问题 |
| 9.6 | 以上但有 1 处重复用词 |
| 9.5 | 语言流畅；中文有 1 处生硬（如欧化句式）；英文有 1 处翻译腔 |
| 9.4 | 语言流畅；有 2 处生硬表达 |
| 9.0 | 语言流畅但整体风格不一致（如突然口语/书面混杂） |
| 8.0 | 有 1 处语法错误或错别字 |
| 7.0 | 有 2 处语法/拼写错误 |
| 6.0 | 多处语言问题影响阅读 |
| 5.0 | 需要大幅度修改才能读通 |
| 4.0 | 中文很机器味 / 英文明显机翻 |
| 3.0 | 难以理解 |
| 2.0 | 基本读不懂 |
| 1.0 | 完全不通顺 |
| 0.0 | 空内容 |

### 3. SEO 覆盖度

| 分值 | 标准 |
|------|------|
| 10.0 | 核心关键词在前 50 字内出现；title/description/正文/h1/h2/标签 6 层覆盖；同义词/长尾词自然嵌入 |
| 9.9 | 以上但关键词在 60 字内出现 |
| 9.8 | 6 层覆盖但关键词在 80 字内出现 |
| 9.7 | 6 层覆盖但有 1 处关键词堆砌感 |
| 9.6 | 核心词覆盖 5 层（缺标签或 description 之一） |
| 9.5 | 核心词覆盖 4 层（title + h1 + 正文 + 标签）；正文前 100 字内出现 |
| 9.4 | 核心词覆盖 4 层但首次出现超过 100 字 |
| 9.0 | 核心词覆盖 3 层 |
| 8.0 | 核心词覆盖 2 层；缺关键词策略 |
| 7.0 | 仅 title 和 description 有关键词 |
| 6.0 | 仅 description 有关键词 |
| 5.0 | 只有 title 有关键词 |
| 4.0 | 无关键词策略 |
| 3.0 | 标题里都没有 |
| 2.0 | 完全没有关键词 |
| 1.0 | 无任何 SEO meta |
| 0.0 | 不适用 |

### 4. 可读性与结构

| 分值 | 标准 |
|------|------|
| 10.0 | 段落 ≤ 5 行；h2/h3 层级 ≤ 3 级；有流程图/Mermaid/表格/代码块交替；扫描式阅读无障碍 |
| 9.9 | 以上但有 1 个段落 ≥ 6 行 |
| 9.8 | 以上但缺表格/Mermaid 其中之一 |
| 9.7 | 结构清晰；段落大多 ≤ 6 行；缺 1 处视觉分割（如图表） |
| 9.6 | 结构清晰；有 2 个段落 ≥ 7 行 |
| 9.5 | 结构合理；h2/h3 正确分层；有代码块；段落多数 ≤ 8 行 |
| 9.4 | 结构合理；有 1 处层级混乱（如 h2 下无 h3 直接到 h4） |
| 9.0 | 结构基本合理；段落偏长（平均 8-10 行） |
| 8.0 | 缺少分段；大段文字堆砌 |
| 7.0 | 层级跳级严重；无法扫描式阅读 |
| 6.0 | 无 Mermaid/表格/代码块等视觉元素 |
| 5.0 | 段落平均 10 行以上 |
| 4.0 | 只有文字无图无表 |
| 3.0 | 结构混乱 |
| 2.0 | 几乎无结构 |
| 1.0 | 完全无排版 |
| 0.0 | 无内容 |

#### Mermaid 图表渲染（强制项，附加在第 4 项上）

> 凡是用了 Mermaid 图表的文章，发布前必须人工目测一遍，**任何一项不通过都直接打回**：

- ✅ **无 parse error** —— 浏览器无红色错误块；控制台无 `Mermaid render error`
- ✅ **所有节点都画出来** —— 不会因为布局空间不够而消失或重叠
- ✅ **所有边都画出来** —— LR/TB 图都不会丢边（这是 Mermaid 多源多汇图的常见坑）
- ✅ **中文字符不炸** —— 节点 label 里的中文/特殊字符要显示正常，不出现 `□□□` 或乱码
- ✅ **语法匹配当前 Mermaid 版本** —— v11 已废弃 `flowchart TB` 之外的某些语法，迁到 11 时全部检查一遍

实现方式：所有 Mermaid 一律用 `<Mermaid client:load chart={...} />` 组件（已在 `src/components/Blog/Mermaid.tsx`），不要用 ```mermaid 围栏（围栏代码块在 MDX 里默认不渲染成图）。

### 5. 代码与示例质量

| 分值 | 标准 |
|------|------|
| 10.0 | 代码可运行；有正确 lang 标签（`bash`/`tsx`/`jsonc` 等）；缩进一致；无过时语法；有输入输出示例 |
| 9.9 | 以上但 1 个 lang 标签可优化（如 `json` 应为 `jsonc`） |
| 9.8 | 以上但缺少 1 处输出示例 |
| 9.7 | 代码可运行；lang 标签正确；有 1 处格式缺陷（缩进/换行不一致） |
| 9.6 | 代码可运行；lang 标签正确；缺少输入输出示例 |
| 9.5 | 代码可运行；lang 标签正确；无格式问题；但无输出示例也无注释 |
| 9.4 | 代码可运行；1 处 lang 标签错误（如 `json` 但有注释） |
| 9.0 | 代码可运行；2+ 处 lang 标签错误 |
| 8.0 | 代码有明显弃用 API |
| 7.0 | 代码不完整（缺关键步骤） |
| 6.0 | 代码有语法错误 |
| 5.0 | 代码与描述不符 |
| 4.0 | 代码不在上下文中 |
| 3.0 | 代码错误多 |
| 2.0 | 几乎不能用 |
| 1.0 | 完全错误 |
| 0.0 | 无代码示例 |

### 6. 深度与实用性

| 分值 | 标准 |
|------|------|
| 10.0 | 有独特踩坑经验；有真实数据或源码分析；有方案对比（优缺点、选型权衡）；读完后能直接动手 |
| 9.9 | 以上但缺 1 处数据支撑（如只说"性能好"没说具体数据） |
| 9.8 | 有踩坑经验 + 方案对比；但缺真实数据 |
| 9.7 | 有踩坑经验；有方案对比但不够深入（只说了面没说因） |
| 9.6 | 有踩坑经验；无方案对比 |
| 9.5 | 有 1 个真实案例或踩坑；解决方案具体可落地 |
| 9.4 | 有真实案例但解决方案不够具体 |
| 9.0 | 有真实案例但无个人观点（纯描述） |
| 8.0 | 内容准确但无个人经验；纯资料整理 |
| 7.0 | 内容浅显；网上随便能搜到 |
| 6.0 | 官方文档的简化翻译 |
| 5.0 | 观点主观无依据 |
| 4.0 | 内容空洞 |
| 3.0 | 几乎没内容 |
| 2.0 | 篇幅太短 |
| 1.0 | 无价值 |
| 0.0 | 不适用 |

### 7. 文档引用与链接

| 分值 | 标准 |
|------|------|
| 10.0 | 所有关键陈述有官方文档引用；链接有效且指向具体章节（非首页）；版本号/日期标注清晰 |
| 9.9 | 以上但有 1 个链接指向首页而非具体章节 |
| 9.8 | 以上但缺 1 处版本号标注 |
| 9.7 | 引用完整；链接有效；但无版本号标注 |
| 9.6 | 引用完整；但有 1 个链接指向泛页面（首页/索引页） |
| 9.5 | 关键陈述有引用；链接有效；无版本号 |
| 9.4 | 关键陈述有引用；但有 1 个过时链接（重定向） |
| 9.0 | 有引用但链接不精确（指向目录/首页） |
| 8.0 | 有引用但无具体链接 |
| 7.0 | 只有 1-2 个引用 |
| 6.0 | 无引用但陈述都为常识 |
| 5.0 | 关键点缺少引用 |
| 4.0 | 引用来源不可靠（个人博客） |
| 3.0 | 引用过少 |
| 2.0 | 基本无引用 |
| 1.0 | 完全无引用 |
| 0.0 | 不适用 |

### 8. 中英文一致性

| 分值 | 标准 |
|------|------|
| 10.0 | 版本完全对应；段落级别对齐；无遗漏无多出；术语翻译一致；英文非直译而是按英文习惯重写 |
| 9.9 | 以上但有 1 处术语不一致（如"向量数据库" vs "vector store"） |
| 9.8 | 以上但英文有 1 处直译痕迹 |
| 9.7 | 中英文对应；术语一致；但英文整体偏直译 |
| 9.6 | 中英文对应；术语一致；英文有 2 处直译痕迹 |
| 9.5 | 版本对应；无遗漏段落；术语统一；英文能读 |
| 9.4 | 版本对应；英文有 3+ 处直译 |
| 9.0 | 中英文一致；但英文可读性一般 |
| 8.0 | 中英文结构一致但英文有明显机翻感 |
| 7.0 | 主要段落对应但有遗漏/多出小节 |
| 6.0 | 中英文不一致（如英文版本短一截） |
| 5.0 | 只有部分翻译 |
| 4.0 | 有缺失章节 |
| 3.0 | 大量缺失 |
| 2.0 | 机翻未校对 |
| 1.0 | 英文完全不对应 |
| 0.0 | 只有单语版本 |

### 9. 工程完整性

| 分值 | 标准 |
|------|------|
| 10.0 | `astro build` 通过；0 个 TypeScript 错误；0 个死链接；meta 标签完整；所有图片正常加载；每篇博客 zh+en 均含 `[data-view-count]` 且部署后实测被 GoatCounter 回填（非恒为 0） |
| 9.5 | 构建通过；无 TS error；meta 标签基本完整；但某篇博客漏接 `<ViewCount />` 或部署后实测 `[data-view-count]` 恒为 0（浏览次数未生效） |
| 9.9 | 以上但有 1 个无伤大雅的 TS 类型警告（非 error） |
| 9.8 | 以上但有 1 个 meta 标签缺失（如缺少 og:image） |
| 9.7 | 构建通过；无 TS error；但有 1 个外部链接返回 301 |
| 9.6 | 构建通过；无 TS error；但有 1 个死图（404 图片引用） |
| 9.5 | 构建通过；无 TS error；meta 标签基本完整 |
| 9.4 | 构建通过；无 TS error；但有 2+ 个 meta 缺失 |
| 9.0 | 构建通过；无 TS error；有 1 个死链接 |
| 8.0 | 构建通过；有 TS error 但不致命 |
| 7.0 | 构建通过；有死链接 + TS warning |
| 6.0 | 构建有 warning |
| 5.0 | 构建失败但部分输出可用 |
| 4.0 | 构建失败（配置问题） |
| 3.0 | 构建失败（代码问题） |
| 2.0 | 无法构建 |
| 1.0 | 无构建配置 |
| 0.0 | 不适用 |

### 10. 设计系统一致性

| 分值 | 标准 |
|------|------|
| 10.0 | 只用 CSS 变量（`var(--space-N)`、`var(--color-xxx)`）；无硬编码颜色/间距；新组件与已有风格一致 |
| 9.9 | 以上但有 1 处用了 CSS 变量但值不合适（如用 `--space-8` 替代 `--space-6`） |
| 9.8 | 以上但新元素不符合响应式断点（移动端优先） |
| 9.7 | 主要用 CSS 变量；有 1 处硬编码间距（`padding: 8px`） |
| 9.6 | 主要用 CSS 变量；有 1 处硬编码颜色 |
| 9.5 | 主要用 CSS 变量；硬编码值 ≤ 2 处且皆为细微问题 |
| 9.4 | 主要用 CSS 变量；3+ 处硬编码 |
| 9.0 | 混合使用 CSS 变量和硬编码 |
| 8.0 | 大量硬编码但风格与现有一致 |
| 7.0 | 新增组件不与已有组件复用 |
| 6.0 | 破坏现有风格 |
| 5.0 | 不一致的颜色/字体选择 |
| 4.0 | 样式冲突 |
| 3.0 | 覆盖全局样式 |
| 2.0 | 引入新设计语言 |
| 1.0 | 无设计系统概念 |
| 0.0 | 不适用 |
