---
title: "Starship Terminal Prompt: Install and Usage Guide"
description: "Starship is a Rust-based cross-platform terminal prompt with millisecond startup. This guide covers installation and basic configuration on macOS and Windows."
pubDate: 2026-07-07
tags: ['terminal', 'macos', 'windows', 'starship', 'developer-tools']
---

## Introduction

Starship is a cross-platform terminal prompt written in Rust — **fast** (< 10ms startup), **out of the box**, and supports Bash, Zsh, Fish, PowerShell, and more.

Key features:
- **Fast**: < 10ms startup, almost imperceptible
- **Cross-platform**: Same experience on macOS, Linux, and Windows
- **Config-as-code**: A single `starship.toml` manages everything
- **Smart detection**: Auto-displays Git, Node.js, Python, Docker, and more

## macOS Installation

### 1. Install Starship

```bash
brew install starship
```

### 2. Initialize in Shell Config

**Zsh** (`~/.zshrc`):
```bash
eval "$(starship init zsh)"
```

**Bash** (`~/.bashrc`):
```bash
eval "$(starship init bash)"
```

**Fish** (`~/.config/fish/config.fish`):
```fish
starship init fish | source
```

### 3. Verify

Restart the terminal and run:

```bash
starship --version
```

**Expected output**: `starship 1.x.x`

### 4. Uninstall

```bash
brew uninstall starship
```

Then remove the `eval "$(starship init ...)"` line from your shell config.

## Windows Installation

### Method 1: Scoop (Recommended)

```powershell
scoop install starship
```

### Method 2: winget

```powershell
winget install --id Starship.Starship
```

### Method 3: Manual

Download `starship-x86_64-pc-windows-msvc.zip` from [GitHub Releases](https://github.com/starship/starship/releases), extract to `C:\Program Files\starship\`, and add it to your PATH.

### Initialize in PowerShell

Edit `$PROFILE`:

```powershell
Invoke-Expression (&starship init powershell)
```

### Verify

```powershell
starship --version
```

## Linux Installation

```bash
curl -sS https://starship.rs/install.sh | sh
```

The script detects your architecture and installs to `~/.local/bin/`. Make sure that directory is in your PATH, then add `eval "$(starship init zsh)"` to `~/.zshrc` or `~/.bashrc`.

## Font Configuration (Nerd Font)

Starship uses Nerd Font icons, so you need one installed.

### macOS

```bash
brew install --cask font-fira-code-nerd-font
brew install --cask font-jetbrains-mono-nerd-font
```

### Windows

Download and install JetBrains Mono Nerd Font or Fira Code Nerd Font from [Nerd Fonts Releases](https://github.com/ryanoasis/nerd-fonts/releases).

### Configure Your Terminal

Set the font in your terminal settings to the Nerd Font you just installed.

## Basic Configuration

Config file location:
- **macOS / Linux**: `~/.config/starship.toml`
- **Windows**: `%USERPROFILE%\.config\starship.toml`

### Minimal Config

```bash
mkdir -p ~/.config && touch ~/.config/starship.toml
```

```toml
# starship.toml
add_newline = false

[character]
success_symbol = "[➜](bold green)"
error_symbol = "[➜](bold red)"
```

### Common Modules

```toml
[directory]
style = "bold cyan"
truncation_length = 3
truncation_symbol = "…/"

[git_branch]
symbol = "🌱 "

[git_status]
ahead = "⇡${count}"
behind = "⇣${count}"

[nodejs]
symbol = "⬢ "
style = "bold green"

[python]
symbol = "🐍 "
style = "bold yellow"
```

### Using Presets

Starship offers several [official presets](https://starship.rs/presets/):

```bash
# List available presets
starship preset --list

# Apply a preset
starship preset tokyo-night > ~/.config/starship.toml
```

Popular presets: `tokyo-night`, `gruvbox-rainbow`, `pastel-powerline`, `pure-preset`.

## Troubleshooting

### Garbled characters

**Cause**: Nerd Font not installed. See the font configuration section above.

### Module not showing

```bash
starship explain   # Check current config
starship print     # Print the current prompt
```

### Slow startup

```bash
starship timings   # Check module timing
```

Disable unused modules to optimize:

```toml
[package]
disabled = true

[python]
disabled = true
```

### Wrong colors

Ensure your terminal supports true color:

```bash
echo $COLORTERM
# Expected: truecolor or 256color
```

## References

- [Starship Official Documentation](https://starship.rs/)
- [Starship Presets](https://starship.rs/presets/)
- [Nerd Fonts](https://www.nerdfonts.com/)
- [Starship GitHub](https://github.com/starship/starship)
